name: Build

on:
  push:
    branches: [main]
    tags: ["v*"]            # vX.Y.Z (optionally -rc1 etc.) -> build + draft a release
    # Skip the 3-platform build for docs/site-only pushes to main (no [skip ci]
    # needed). Only files that don't affect the binary are listed — never src/,
    # cmake/, CMakeLists.txt or the bundlers. Release tags still build: the
    # release ritual bumps PROJECT_VERSION in CMakeLists.txt and tags that
    # commit, so a non-ignored file always changes.
    paths-ignore:
      - "docs/**"
      - "site/**"
      - "**/*.md"
      - "LICENSE"
      - ".github/workflows/pages.yml"
  pull_request:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      linux:
        description: "Build Linux (x86_64)"
        type: boolean
        default: true
      linux_arm64:
        description: "Build Linux (arm64)"
        type: boolean
        default: true
      windows:
        description: "Build Windows"
        type: boolean
        default: true
      macos:
        description: "Build macOS (arm64)"
        type: boolean
        default: true

# Driver versions — bump both here when upgrading
env:
  MONGOC_VERSION: "2.3.0"
  MONGOCXX_VERSION: "4.3.1"

jobs:

  # ──────────────────────────────────────────────────────────────────────────
  # Linux — apt packages; produces tarball via bin/bundle-linux.sh
  # ──────────────────────────────────────────────────────────────────────────
  linux:
    name: Linux (Ubuntu 24.04)
    runs-on: ubuntu-24.04
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.linux }}

    steps:
      - uses: actions/checkout@v6

      - name: Install Qt6 and system deps
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            ninja-build \
            qt6-base-dev \
            libqscintilla2-qt6-dev \
            libssl-dev \
            libssh2-1-dev \
            pkg-config

      # ── mongo-cxx-driver ──────────────────────────────────────────────────
      # We build from source to match the exact 4.x ABI used locally.
      # The install prefix is cached so subsequent runs skip the build.

      - name: Cache mongocxx install
        id: cache-mongocxx
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/mongocxx-prefix
          key: mongocxx-${{ env.MONGOCXX_VERSION }}-mongoc-${{ env.MONGOC_VERSION }}-ubuntu24.04-v1

      - name: Build mongo-c-driver
        if: steps.cache-mongocxx.outputs.cache-hit != 'true'
        run: |
          wget -q "https://github.com/mongodb/mongo-c-driver/releases/download/${{ env.MONGOC_VERSION }}/mongo-c-driver-${{ env.MONGOC_VERSION }}.tar.gz"
          tar xf "mongo-c-driver-${{ env.MONGOC_VERSION }}.tar.gz"
          cmake -S "mongo-c-driver-${{ env.MONGOC_VERSION }}" -B build-mongoc -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}/mongocxx-prefix" \
            -DENABLE_AUTOMATIC_INIT_AND_CLEANUP=OFF \
            -DENABLE_TESTS=OFF \
            -DENABLE_EXAMPLES=OFF
          cmake --build build-mongoc
          cmake --install build-mongoc

      - name: Build mongo-cxx-driver
        if: steps.cache-mongocxx.outputs.cache-hit != 'true'
        run: |
          wget -q "https://github.com/mongodb/mongo-cxx-driver/releases/download/r${{ env.MONGOCXX_VERSION }}/mongo-cxx-driver-r${{ env.MONGOCXX_VERSION }}.tar.gz"
          tar xf "mongo-cxx-driver-r${{ env.MONGOCXX_VERSION }}.tar.gz"
          cmake -S "mongo-cxx-driver-r${{ env.MONGOCXX_VERSION }}" -B build-mongocxx -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}/mongocxx-prefix" \
            -DCMAKE_PREFIX_PATH="${{ github.workspace }}/mongocxx-prefix"
          cmake --build build-mongocxx
          cmake --install build-mongocxx

      - name: Configure Docutaz
        run: |
          mkdir build
          cmake -S . -B build -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DDOCUTAZ_BUILD_TESTS=ON \
            -DCMAKE_PREFIX_PATH="${{ github.workspace }}/mongocxx-prefix/lib64/cmake;${{ github.workspace }}/mongocxx-prefix/lib/cmake;${{ github.workspace }}/mongocxx-prefix"

      - name: Build
        run: cmake --build build --target docutaz docutaz_unit_tests --parallel

      - name: Expose mongocxx libs for bundler
        # bin/bundle-linux.sh scans /usr/local/lib64; copy the CI-built shared
        # libs there so the bundler's glob finds them. This also satisfies the
        # docutaz_unit_tests rpath (/usr/local/lib64) before ctest runs.
        run: |
          sudo mkdir -p /usr/local/lib64
          sudo find "${{ github.workspace }}/mongocxx-prefix" \
            \( -name "*.so" -o -name "*.so.*" \) \
            -exec cp -Pf {} /usr/local/lib64/ \;

      - name: Run unit tests
        run: ctest --test-dir build --output-on-failure

      - name: Create tarball
        run: bash bin/bundle-linux.sh

      - name: Smoke test (packaged binary launches)
        run: |
          # Extract the tarball and launch the PACKAGED binary via its launcher,
          # exactly as a user would. `--version` constructs the QApplication
          # (loading the platform plugin and every linked library) then exits 0,
          # so this catches startup/packaging regressions — missing libs, a
          # broken bundle, or an ABI-incompatible Qt — that building alone won't.
          # offscreen avoids needing an X server on the runner.
          tar xzf docutaz-*-linux-x86_64.tar.gz
          dir=$(echo docutaz-*-linux-x86_64/)
          out=$(cd "$dir" && QT_QPA_PLATFORM=offscreen ./docutaz.sh --version)
          echo "$out"
          echo "$out" | grep -q "Docutaz" || { echo "ERROR: binary did not launch / print version"; exit 1; }

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: docutaz-linux
          path: docutaz-*-linux-x86_64.tar.gz
          if-no-files-found: error

  # ──────────────────────────────────────────────────────────────────────────
  # Linux arm64 — same recipe as the x86_64 job, on the native arm64 runner.
  # apt has arm64 Qt6/QScintilla; mongocxx builds from source; bundle-linux.sh
  # picks up the aarch64 suffix from `uname -m`. Cache key is arch-specific so
  # it never restores x86_64 mongocxx binaries.
  # ──────────────────────────────────────────────────────────────────────────
  linux-arm64:
    name: Linux (Ubuntu 24.04 arm64)
    runs-on: ubuntu-24.04-arm
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.linux_arm64 }}

    steps:
      - uses: actions/checkout@v6

      - name: Install Qt6 and system deps
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            ninja-build \
            qt6-base-dev \
            libqscintilla2-qt6-dev \
            libssl-dev \
            libssh2-1-dev \
            pkg-config

      - name: Cache mongocxx install
        id: cache-mongocxx
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/mongocxx-prefix
          key: mongocxx-${{ env.MONGOCXX_VERSION }}-mongoc-${{ env.MONGOC_VERSION }}-ubuntu24.04-arm64-v1

      - name: Build mongo-c-driver
        if: steps.cache-mongocxx.outputs.cache-hit != 'true'
        run: |
          wget -q "https://github.com/mongodb/mongo-c-driver/releases/download/${{ env.MONGOC_VERSION }}/mongo-c-driver-${{ env.MONGOC_VERSION }}.tar.gz"
          tar xf "mongo-c-driver-${{ env.MONGOC_VERSION }}.tar.gz"
          cmake -S "mongo-c-driver-${{ env.MONGOC_VERSION }}" -B build-mongoc -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}/mongocxx-prefix" \
            -DENABLE_AUTOMATIC_INIT_AND_CLEANUP=OFF \
            -DENABLE_TESTS=OFF \
            -DENABLE_EXAMPLES=OFF
          cmake --build build-mongoc
          cmake --install build-mongoc

      - name: Build mongo-cxx-driver
        if: steps.cache-mongocxx.outputs.cache-hit != 'true'
        run: |
          wget -q "https://github.com/mongodb/mongo-cxx-driver/releases/download/r${{ env.MONGOCXX_VERSION }}/mongo-cxx-driver-r${{ env.MONGOCXX_VERSION }}.tar.gz"
          tar xf "mongo-cxx-driver-r${{ env.MONGOCXX_VERSION }}.tar.gz"
          cmake -S "mongo-cxx-driver-r${{ env.MONGOCXX_VERSION }}" -B build-mongocxx -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}/mongocxx-prefix" \
            -DCMAKE_PREFIX_PATH="${{ github.workspace }}/mongocxx-prefix"
          cmake --build build-mongocxx
          cmake --install build-mongocxx

      - name: Configure Docutaz
        run: |
          mkdir build
          cmake -S . -B build -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DDOCUTAZ_BUILD_TESTS=ON \
            -DCMAKE_PREFIX_PATH="${{ github.workspace }}/mongocxx-prefix/lib64/cmake;${{ github.workspace }}/mongocxx-prefix/lib/cmake;${{ github.workspace }}/mongocxx-prefix"

      - name: Build
        run: cmake --build build --target docutaz docutaz_unit_tests --parallel

      - name: Expose mongocxx libs for bundler
        run: |
          sudo mkdir -p /usr/local/lib64
          sudo find "${{ github.workspace }}/mongocxx-prefix" \
            \( -name "*.so" -o -name "*.so.*" \) \
            -exec cp -Pf {} /usr/local/lib64/ \;

      - name: Run unit tests
        run: ctest --test-dir build --output-on-failure

      - name: Create tarball
        run: bash bin/bundle-linux.sh

      - name: Smoke test (packaged binary launches)
        run: |
          tar xzf docutaz-*-linux-aarch64.tar.gz
          dir=$(echo docutaz-*-linux-aarch64/)
          out=$(cd "$dir" && QT_QPA_PLATFORM=offscreen ./docutaz.sh --version)
          echo "$out"
          echo "$out" | grep -q "Docutaz" || { echo "ERROR: binary did not launch / print version"; exit 1; }

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: docutaz-linux-arm64
          path: docutaz-*-linux-aarch64.tar.gz
          if-no-files-found: error

  # ──────────────────────────────────────────────────────────────────────────
  # Windows — Qt via aqtinstall (pre-built), mongocxx built from source
  #
  # Why not vcpkg for Qt?  vcpkg builds Qt from source which takes 30-60 min
  # per run even with binary caching on first miss.  aqtinstall downloads the
  # same pre-built Qt binaries that Qt's installer uses — ~2 minutes.
  # To switch to full vcpkg, delete the install-qt-action step and add
  # qtbase + qtsvg to vcpkg.json (note: that also drags QScintilla's Qt build).
  # ──────────────────────────────────────────────────────────────────────────
  windows:
    name: Windows (x64)
    runs-on: windows-latest
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.windows }}

    # ilammy/msvc-dev-cmd has no Node 24 release yet (latest v1.13.0 still
    # declares node20). Opt this job into Node 24 so the action runs on the
    # supported runtime ahead of GitHub's 2026-06-16 forced cutover. The other
    # actions in this job are already node24, so this only affects msvc-dev-cmd.
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - uses: actions/checkout@v6

      - name: Install Qt 6
        uses: jurplel/install-qt-action@v4
        with:
          version: "6.8.1"
          arch: win64_msvc2022_64
          cache: true

      # Activate the MSVC toolchain so Ninja compiles with cl.exe (matching the
      # MSVC-ABI Qt above). Without this, CMake picks up the MinGW gcc that ships
      # on the runner's PATH, producing an ABI mismatch against Qt/mongocxx.
      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      # QScintilla isn't shipped by aqtinstall and the vcpkg port would pull a
      # full vcpkg Qt build (defeating aqtinstall). Build it from source against
      # the aqtinstall Qt6 with qmake/nmake and install into the Qt prefix, so
      # the build's find_path/find_library locate Qsci headers + qscintilla2_qt6.
      - name: Build QScintilla (Qt6, from source)
        shell: pwsh
        env:
          QSCI_VERSION: "2.14.1"
        run: |
          $url = "https://www.riverbankcomputing.com/static/Downloads/QScintilla/$env:QSCI_VERSION/QScintilla_src-$env:QSCI_VERSION.tar.gz"
          curl.exe -L -o qscintilla.tar.gz $url
          tar -xf qscintilla.tar.gz
          cd "QScintilla_src-$env:QSCI_VERSION/src"
          qmake qscintilla.pro
          nmake
          nmake install
          # qmake's install location for the runtime DLL varies across versions
          # (the import .lib reliably lands in the Qt lib dir, but the DLL may be
          # left in the build tree or placed in lib). Copy it deterministically
          # into the Qt bin dir, which is on the build's CMAKE_PREFIX_PATH, the
          # ctest PATH, and where windeployqt/packaging look. Fail loudly if the
          # DLL was never produced rather than shipping a broken artifact.
          $dll = Get-ChildItem -Path ".","$env:QT_ROOT_DIR" -Recurse -Filter "qscintilla2_qt6.dll" -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $dll) { throw "qscintilla2_qt6.dll not found after build/install" }
          Copy-Item $dll.FullName -Destination "$env:QT_ROOT_DIR\bin" -Force
          Write-Host "Placed qscintilla2_qt6.dll (from $($dll.FullName)) into $env:QT_ROOT_DIR\bin"

      # vcpkg provides OpenSSL and libssh2 — both pinned via the
      # builtin-baseline in ci/vcpkg/vcpkg.json. find_package(Libssh2) and our
      # cmake/FindOpenSSL.cmake locate them via CMAKE_PREFIX_PATH (set in
      # Configure). mongo-c-driver uses Windows SChannel, not OpenSSL, so
      # OpenSSL here is only for the libssh2 / ssh / docutaz link.
      - name: Cache vcpkg deps
        id: cache-vcpkg
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}\vcpkg-deps
          key: vcpkg-deps-x64-windows-${{ hashFiles('ci/vcpkg/vcpkg.json') }}-v1

      - name: Install OpenSSL + libssh2 (vcpkg, pinned)
        if: steps.cache-vcpkg.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # The pinned builtin-baseline (ci/vcpkg/vcpkg.json) is usually newer
          # than the commit in the runner's preinstalled vcpkg, so manifest
          # resolution can't read versions/baseline.json at that commit. Fetch
          # so the baseline commit and its registry objects are available.
          git -C "$env:VCPKG_INSTALLATION_ROOT" fetch --quiet origin
          # Manifest-mode install from ci/vcpkg/vcpkg.json (a dedicated manifest,
          # NOT the repo root, so mongo-cxx-driver is not pulled in — we build
          # that separately). The manifest's builtin-baseline pins all versions.
          & "$env:VCPKG_INSTALLATION_ROOT\vcpkg.exe" install `
            --vcpkg-root="$env:VCPKG_INSTALLATION_ROOT" `
            --x-manifest-root="$env:GITHUB_WORKSPACE\ci\vcpkg" `
            --triplet x64-windows `
            --x-install-root="$env:GITHUB_WORKSPACE\vcpkg-deps"

      - name: Cache mongocxx install
        id: cache-mongocxx
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}\mongocxx-prefix
          key: mongocxx-${{ env.MONGOCXX_VERSION }}-mongoc-${{ env.MONGOC_VERSION }}-windows-msvc-v2

      - name: Build mongo-c-driver (Windows)
        if: steps.cache-mongocxx.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          Invoke-WebRequest -Uri "https://github.com/mongodb/mongo-c-driver/releases/download/$env:MONGOC_VERSION/mongo-c-driver-$env:MONGOC_VERSION.tar.gz" -OutFile mongoc.tar.gz
          tar xf mongoc.tar.gz
          cmake -S "mongo-c-driver-$env:MONGOC_VERSION" -B build-mongoc -GNinja `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl `
            -DCMAKE_INSTALL_PREFIX="$env:GITHUB_WORKSPACE\mongocxx-prefix" `
            -DENABLE_AUTOMATIC_INIT_AND_CLEANUP=OFF `
            -DENABLE_TESTS=OFF `
            -DENABLE_EXAMPLES=OFF
          cmake --build build-mongoc
          cmake --install build-mongoc

      - name: Build mongo-cxx-driver (Windows)
        if: steps.cache-mongocxx.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          Invoke-WebRequest -Uri "https://github.com/mongodb/mongo-cxx-driver/releases/download/r$env:MONGOCXX_VERSION/mongo-cxx-driver-r$env:MONGOCXX_VERSION.tar.gz" -OutFile mongocxx.tar.gz
          tar xf mongocxx.tar.gz
          cmake -S "mongo-cxx-driver-r$env:MONGOCXX_VERSION" -B build-mongocxx -GNinja `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl `
            -DCMAKE_INSTALL_PREFIX="$env:GITHUB_WORKSPACE\mongocxx-prefix" `
            -DCMAKE_PREFIX_PATH="$env:GITHUB_WORKSPACE\mongocxx-prefix"
          cmake --build build-mongocxx
          cmake --install build-mongocxx

      - name: Configure Docutaz (Windows)
        shell: pwsh
        run: |
          cmake -S . -B build -GNinja `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl `
            -DDOCUTAZ_BUILD_TESTS=ON `
            -DCMAKE_PREFIX_PATH="$env:QT_ROOT_DIR;$env:GITHUB_WORKSPACE\mongocxx-prefix\lib\cmake;$env:GITHUB_WORKSPACE\mongocxx-prefix\cmake;$env:GITHUB_WORKSPACE\mongocxx-prefix;$env:GITHUB_WORKSPACE\vcpkg-deps\x64-windows"

      - name: Build
        run: cmake --build build --target docutaz docutaz_unit_tests --parallel

      - name: Run unit tests
        shell: pwsh
        run: |
          # The test binary loads Qt, QScintilla, mongocxx and the vcpkg
          # OpenSSL/libssh2 DLLs at runtime. Windows has no rpath, so put every
          # runtime DLL directory on PATH before invoking ctest. Note QScintilla
          # installs its DLL into the Qt lib dir (qmake target.path =
          # QT_INSTALL_LIBS), not bin, so both Qt dirs are needed.
          $env:PATH = "$env:QT_ROOT_DIR\bin;" +
            "$env:QT_ROOT_DIR\lib;" +
            "$env:GITHUB_WORKSPACE\mongocxx-prefix\bin;" +
            "$env:GITHUB_WORKSPACE\vcpkg-deps\x64-windows\bin;" +
            $env:PATH
          ctest --test-dir build --output-on-failure

      - name: Read version
        id: ver
        shell: pwsh
        run: |
          $txt = Get-Content CMakeLists.txt -Raw
          $major = [regex]::Match($txt, 'PROJECT_VERSION_MAJOR\s+"(\d+)"').Groups[1].Value
          $minor = [regex]::Match($txt, 'PROJECT_VERSION_MINOR\s+"(\d+)"').Groups[1].Value
          $patch = [regex]::Match($txt, 'PROJECT_VERSION_PATCH\s+"(\d+)"').Groups[1].Value
          "version=$major.$minor.$patch" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Package (Windows)
        shell: pwsh
        run: |
          $ver = "${{ steps.ver.outputs.version }}"
          $outDir = "docutaz-$ver-windows-x86_64"
          New-Item -ItemType Directory -Path $outDir | Out-Null
          Copy-Item build\src\docutaz\docutaz.exe $outDir\
          # QScintilla is built from source (not a Qt module), so windeployqt
          # will not copy it on its own. Place it next to the exe first, then
          # hand it to windeployqt alongside docutaz.exe so QScintilla's own Qt
          # dependencies (e.g. Qt6PrintSupport) are deployed too.
          Copy-Item "$env:QT_ROOT_DIR\bin\qscintilla2_qt6.dll" $outDir\
          windeployqt --dir $outDir $outDir\docutaz.exe $outDir\qscintilla2_qt6.dll
          # mongocxx runtime DLLs
          $mongoBin = "$env:GITHUB_WORKSPACE\mongocxx-prefix\bin"
          if (Test-Path $mongoBin) {
            Get-ChildItem $mongoBin -Filter "*.dll" | Copy-Item -Destination $outDir
          }
          # libssh2 + OpenSSL + their transitive runtime DLLs (e.g. zlib/zstd),
          # all linked dynamically. Copy the whole vcpkg bin so nothing the
          # binary loads at startup is missing on a clean machine (a missing
          # transitive DLL shows up only as STATUS_DLL_NOT_FOUND / 0xC0000135).
          $vcpkgBin = "$env:GITHUB_WORKSPACE\vcpkg-deps\x64-windows\bin"
          if (Test-Path $vcpkgBin) {
            Get-ChildItem $vcpkgBin -Filter "*.dll" | Copy-Item -Destination $outDir
          }
          Compress-Archive -Path $outDir -DestinationPath "docutaz-$ver-windows-x86_64.zip"

      - name: Smoke test (packaged binary launches)
        shell: pwsh
        run: |
          # Launch the PACKAGED exe with its windeployqt'd DLLs alongside it.
          # --version constructs the QApplication (loading the platform plugin
          # and every linked DLL) then exits 0, catching deploy regressions
          # (a missing DLL such as qscintilla2_qt6.dll or a Qt module) that the
          # build step alone cannot.
          $ver = "${{ steps.ver.outputs.version }}"
          $outDir = "docutaz-$ver-windows-x86_64"
          # windeployqt deploys qwindows but NOT qoffscreen; add it so the app can
          # launch headless on the runner. It depends only on the Qt6 DLLs already
          # in $outDir. (Done post-zip, so the uploaded artifact stays unchanged.)
          Copy-Item "$env:QT_ROOT_DIR\plugins\platforms\qoffscreen.dll" "$outDir\platforms\" -Force
          $env:QT_QPA_PLATFORM = "offscreen"
          $out = & "$outDir\docutaz.exe" --version 2>&1 | Out-String
          $code = $LASTEXITCODE
          Write-Host "exit=$code output=$out"
          if ($out -notmatch "Docutaz") {
            Write-Host "=== package contents ($outDir) ==="
            Get-ChildItem -Recurse $outDir | Select-Object FullName | Out-String | Write-Host
            throw "binary did not launch / print version (exit $code)"
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: docutaz-windows
          path: docutaz-*-windows-x86_64.zip
          if-no-files-found: error

  # ──────────────────────────────────────────────────────────────────────────
  # macOS (Apple Silicon / arm64) — native Qt6 via Homebrew qt, mongocxx
  # built from source. Homebrew qt/qscintilla2 are native arm64 on Apple Silicon.
  # ──────────────────────────────────────────────────────────────────────────
  macos:
    name: macOS (Apple Silicon / arm64)
    runs-on: macos-14   # Apple Silicon (arm64) runner
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.macos }}

    env:
      # Homebrew's qt (Qt 6) and qscintilla2 are compiled natively for arm64 on
      # Apple Silicon.
      MACOS_ARCH: arm64
      MACOS_DEPLOYMENT_TARGET: "11.0"

    steps:
      - uses: actions/checkout@v6

      - name: Install build tools and native arm64 Qt 6
        run: brew install ninja qt qscintilla2 libssh2 pkg-config

      - name: Expose Qt prefix
        run: echo "QT_PREFIX=$(brew --prefix qt)" >> "$GITHUB_ENV"

      - name: Cache mongocxx install
        id: cache-mongocxx
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/mongocxx-prefix
          key: mongocxx-${{ env.MONGOCXX_VERSION }}-mongoc-${{ env.MONGOC_VERSION }}-macos14-arm64-v1

      - name: Build mongo-c-driver (macOS)
        if: steps.cache-mongocxx.outputs.cache-hit != 'true'
        run: |
          curl -fsSL -o mongoc.tar.gz \
            "https://github.com/mongodb/mongo-c-driver/releases/download/${{ env.MONGOC_VERSION }}/mongo-c-driver-${{ env.MONGOC_VERSION }}.tar.gz"
          tar xf mongoc.tar.gz
          cmake -S "mongo-c-driver-${{ env.MONGOC_VERSION }}" -B build-mongoc -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES="${{ env.MACOS_ARCH }}" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET="${{ env.MACOS_DEPLOYMENT_TARGET }}" \
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}/mongocxx-prefix" \
            -DENABLE_AUTOMATIC_INIT_AND_CLEANUP=OFF \
            -DENABLE_TESTS=OFF \
            -DENABLE_EXAMPLES=OFF
          cmake --build build-mongoc
          cmake --install build-mongoc

      - name: Build mongo-cxx-driver (macOS)
        if: steps.cache-mongocxx.outputs.cache-hit != 'true'
        run: |
          curl -fsSL -o mongocxx.tar.gz \
            "https://github.com/mongodb/mongo-cxx-driver/releases/download/r${{ env.MONGOCXX_VERSION }}/mongo-cxx-driver-r${{ env.MONGOCXX_VERSION }}.tar.gz"
          tar xf mongocxx.tar.gz
          cmake -S "mongo-cxx-driver-r${{ env.MONGOCXX_VERSION }}" -B build-mongocxx -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES="${{ env.MACOS_ARCH }}" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET="${{ env.MACOS_DEPLOYMENT_TARGET }}" \
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}/mongocxx-prefix" \
            -DCMAKE_PREFIX_PATH="${{ github.workspace }}/mongocxx-prefix"
          cmake --build build-mongocxx
          cmake --install build-mongocxx

      - name: Configure Docutaz (macOS)
        run: |
          # Help pkg-config find Homebrew's libssh2.pc (libssh2 is not keg-only,
          # but set it explicitly so the resolution is robust on the runner).
          export PKG_CONFIG_PATH="$(brew --prefix libssh2)/lib/pkgconfig:${PKG_CONFIG_PATH:-}"
          cmake -S . -B build -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DDOCUTAZ_BUILD_TESTS=ON \
            -DCMAKE_OSX_ARCHITECTURES="${{ env.MACOS_ARCH }}" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET="${{ env.MACOS_DEPLOYMENT_TARGET }}" \
            -DCMAKE_PREFIX_PATH="$QT_PREFIX;$(brew --prefix qscintilla2);$(brew --prefix);${{ github.workspace }}/mongocxx-prefix/lib/cmake;${{ github.workspace }}/mongocxx-prefix/lib64/cmake;${{ github.workspace }}/mongocxx-prefix"

      - name: Build
        run: cmake --build build --target docutaz docutaz_unit_tests --parallel

      - name: Run unit tests
        run: |
          # The test binary links mongocxx from a custom prefix and Qt/QScintilla
          # from Homebrew. Point the dynamic loader at all of them so ctest can
          # launch it (the build-tree rpath usually suffices; this makes it robust).
          export DYLD_FALLBACK_LIBRARY_PATH="${{ github.workspace }}/mongocxx-prefix/lib:$QT_PREFIX/lib:$(brew --prefix qscintilla2)/lib:$(brew --prefix)/lib:${DYLD_FALLBACK_LIBRARY_PATH:-}"
          ctest --test-dir build --output-on-failure

      - name: Read version
        id: ver
        run: |
          V_MAJOR=$(grep 'PROJECT_VERSION_MAJOR' CMakeLists.txt | head -1 | grep -o '"[^"]*"' | tr -d '"')
          V_MINOR=$(grep 'PROJECT_VERSION_MINOR' CMakeLists.txt | head -1 | grep -o '"[^"]*"' | tr -d '"')
          V_PATCH=$(grep 'PROJECT_VERSION_PATCH' CMakeLists.txt | head -1 | grep -o '"[^"]*"' | tr -d '"')
          echo "version=$V_MAJOR.$V_MINOR.$V_PATCH" >> "$GITHUB_OUTPUT"

      - name: Package (macOS)
        run: |
          ver="${{ steps.ver.outputs.version }}"
          cd build/src/docutaz
          "$QT_PREFIX/bin/macdeployqt" Docutaz.app
          zip -r "../../../docutaz-${ver}-macos-arm64.zip" Docutaz.app

      - name: Smoke test (packaged binary launches)
        run: |
          # Launch the macdeployqt'd .app's binary. The bundle is self-contained
          # (Qt + QScintilla + mongocxx dylibs relocated into Contents/Frameworks
          # with fixed rpaths), so this verifies macdeployqt produced a runnable
          # app — not just that it built. --version constructs the QApplication
          # (loading the platform plugin and every framework) then exits 0.
          app="build/src/docutaz/Docutaz.app"
          bin="$app/Contents/MacOS/Docutaz"
          # macdeployqt deploys only the cocoa plugin; the offscreen platform
          # (needed to launch headless on CI) is not bundled. Drop it in next to
          # libqcocoa — it references @rpath/QtGui etc., resolved via the app's
          # bundled frameworks. (Done post-zip, so the artifact stays unchanged.)
          # -L: $QT_PREFIX (brew --prefix qt) is a symlink, so find must follow it.
          off=$(find -L "$QT_PREFIX" -name libqoffscreen.dylib 2>/dev/null | head -1)
          [ -n "$off" ] || { echo "ERROR: libqoffscreen.dylib not found under $QT_PREFIX"; exit 1; }
          cp "$off" "$app/Contents/PlugIns/platforms/"
          # Safety net mirroring the unit-test step: only consulted if the bundled
          # frameworks fail to resolve, so it can't shadow the self-contained app.
          export DYLD_FALLBACK_LIBRARY_PATH="${{ github.workspace }}/mongocxx-prefix/lib:$QT_PREFIX/lib:$(brew --prefix qscintilla2)/lib:$(brew --prefix)/lib:${DYLD_FALLBACK_LIBRARY_PATH:-}"
          out=$(QT_QPA_PLATFORM=offscreen "$bin" --version)
          echo "$out"
          echo "$out" | grep -q "Docutaz" || { echo "ERROR: app did not launch / print version"; exit 1; }

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: docutaz-macos
          path: docutaz-*-macos-arm64.zip
          if-no-files-found: error

  # ──────────────────────────────────────────────────────────────────────────
  # Release — runs only on a vX.Y.Z tag. Verifies the tag matches the project
  # version, then attaches the three platform artifacts to a DRAFT GitHub
  # Release with auto-generated notes (review + publish manually). A tag with a
  # suffix (e.g. v2.1.0-rc1) is flagged as a pre-release.
  #
  #   Release ritual: bump PROJECT_VERSION in CMakeLists.txt -> commit -> merge
  #   to main -> `git tag v<that version>` -> `git push origin v<version>`.
  # ──────────────────────────────────────────────────────────────────────────
  release:
    name: Draft GitHub Release
    needs: [linux, linux-arm64, windows, macos]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-24.04
    permissions:
      contents: write          # required to create the release
    steps:
      - uses: actions/checkout@v6

      - name: Verify tag matches PROJECT_VERSION
        run: |
          MAJOR=$(grep 'PROJECT_VERSION_MAJOR' CMakeLists.txt | head -1 | grep -o '"[^"]*"' | tr -d '"')
          MINOR=$(grep 'PROJECT_VERSION_MINOR' CMakeLists.txt | head -1 | grep -o '"[^"]*"' | tr -d '"')
          PATCH=$(grep 'PROJECT_VERSION_PATCH' CMakeLists.txt | head -1 | grep -o '"[^"]*"' | tr -d '"')
          VER="$MAJOR.$MINOR.$PATCH"
          TAG="${GITHUB_REF_NAME#v}"     # v2.0.0 -> 2.0.0 ; v2.1.0-rc1 -> 2.1.0-rc1
          TAG_BASE="${TAG%%-*}"          # strip any -rc1/-beta suffix for the compare
          echo "tag=$GITHUB_REF_NAME  tag-base=$TAG_BASE  CMakeLists=$VER"
          if [ "$TAG_BASE" != "$VER" ]; then
            echo "::error::Tag $GITHUB_REF_NAME (base $TAG_BASE) != PROJECT_VERSION $VER in CMakeLists.txt. Bump the version, commit, and re-tag so the release, artifacts and binary all agree."
            exit 1
          fi

      - name: Download platform artifacts
        uses: actions/download-artifact@v7
        with:
          path: dist

      - name: Draft release with artifacts
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          generate_release_notes: true
          prerelease: ${{ contains(github.ref_name, '-') }}
          fail_on_unmatched_files: true
          files: |
            dist/docutaz-linux/docutaz-*-linux-x86_64.tar.gz
            dist/docutaz-linux-arm64/docutaz-*-linux-aarch64.tar.gz
            dist/docutaz-windows/docutaz-*-windows-x86_64.zip
            dist/docutaz-macos/docutaz-*-macos-arm64.zip
