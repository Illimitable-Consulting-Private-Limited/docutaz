Third-party dependencies
========================
   
Docutaz automatically compiles and statically links to the following
libraries:

1. QScintilla

You do not need to build them separately.

(libssh2 is no longer bundled — it is provided by the platform package
manager: system on Linux, Homebrew on macOS, vcpkg on Windows.)
